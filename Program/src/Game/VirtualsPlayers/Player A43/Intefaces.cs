using System.Collections.Specialized;
namespace GameProgram
{
    public interface IStrategy43
    {
        double GameStateScore(Game game);
       // double BestMove(List<Card> First, List<Card> Second); // igual que el compare to + 0 -
    }
}